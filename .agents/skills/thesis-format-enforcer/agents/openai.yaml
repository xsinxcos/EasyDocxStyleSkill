interface:
  display_name: "Thesis Format Enforcer"
  short_description: "Build thesis rule packs from DOCX structure plus template prose rules, then validate and fix drafts."
  default_prompt: "Use $thesis-format-enforcer to derive a thesis rule pack from a template DOCX, absorb both the template formatting and any prose-stated formatting requirements, validate it with DOCX package metadata diff, and audit my draft."
